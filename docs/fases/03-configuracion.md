# Configuración

Ajustar lo ya creado: variables, parámetros del servicio, políticas y secretos.

Los valores que cambian por entorno viven en variables de `terraform/envs/dev`. Los secretos no se commitean: `*.tfvars` está en `.gitignore` y hay un ejemplo en `terraform.tfvars.example`.

## Apuntes

